class SmallestIntegerFinder {

findSmallestInt(arr){

    let min = arr[0];
    let temp = 0;

    for(let i=1;i<arr.length;i++)
    {
        if(min >= arr[i]){
          temp = min;
          min = arr[i];
          arr[i] = temp; 
        }
      }
      
      return min;
    }
  }
